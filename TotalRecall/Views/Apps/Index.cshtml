@model List<Models.TotalRecall.Application>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<ul>
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(linkText : item.Name, controllerName : "Apps", actionName:"V", routeValues: new { publicKey = item.PublicKey })</li>
    }
</ul>